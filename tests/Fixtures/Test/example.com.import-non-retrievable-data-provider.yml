config:
  browser: chrome
  url: https://example.com

imports:
  data_providers:
    data_provider_import_name: "../DataProvider/file-does-not-exist.yml"

"verify page is open":
  assertions:
    - $page.url is "https://example.com"
