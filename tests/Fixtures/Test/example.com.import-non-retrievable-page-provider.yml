config:
  browsers:
    - chrome
  url: https://example.com

imports:
  pages:
    page_import_name: "../Page/file-does-not-exist.yml"

"verify page is open":
  assertions:
    - $page.url is "https://example.com"
