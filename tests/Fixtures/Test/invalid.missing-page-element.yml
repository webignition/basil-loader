config:
  browsers:
    - chrome
  url: https://example.com

imports:
  pages:
    page_import_name: "../Page/example.com.button.heading.yml"

"step referencing missing page element":
  actions:
    - click $page_import_name.elements.missing
